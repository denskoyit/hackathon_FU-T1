## Чек-поинт по проделанной работе и дальнейшим перспективам

**Дата: 22.12**

### Общий контекст работы

В рамках задачи оптимизации размещения клиник в городе Северск мы последовательно формализовали проблему, построили базовое решение и заложили фундамент для дальнейшего улучшения качества оптимизации. Работа велась в логике классических задач размещения объектов (Facility Location Problem) с расширением на реальные ограничения и многокритериальность.

Основная цель — определить координаты 250 клиник таким образом, чтобы минимизировать суммарные (или средние) затраты на обслуживание клиентов с учётом:

* расстояния от клиента до ближайшей клиники,
* ограничений по вместимости клиник,
* социальных, демографических и инфраструктурных факторов.

---

## 1. Теоретическая база и формализация задачи

### 1.1. Задача размещения объектов (Facility Location Problem)

Проект был формализован как классическая задача размещения объектов:

* **клиенты** — точки спроса;
* **клиники** — центры обслуживания;
* **цель** — минимизация суммарной стоимости обслуживания.

При этом задача обладает рядом усложняющих факторов:

* ограничение по вместимости клиник (capacitated FLP);
* нелинейная функция стоимости расстояния (линейная + квадратичная компоненты);
* штрафы за перегрузку;
* наличие нескольких конкурирующих критериев качества.

В качестве теоретической основы использовались:

* *p-median problem*,
* *Weber problem*,
* *k-center / k-means approximation*.

---

### 1.2. Кластеризация как приближение оптимума

Размещение клиник естественным образом интерпретируется как задача кластеризации клиентов:

* каждая клиника соответствует центру кластера,
* каждый клиент относится к ближайшему центру.

Идея заключается в том, что центр кластера является приближением оптимального положения клиники для данной группы клиентов. Это позволило использовать методы кластеризации как стартовую эвристику.

---

### 1.3. Многокритериальная оптимизация

Помимо расстояния учитываются дополнительные факторы:

* плотность населения,
* возрастная структура,
* социальная уязвимость,
* инфраструктура,
* экология (близость парков).

Это приводит к необходимости:

* агрегировать признаки в единый скоринг локации (LCS),
* работать с взвешенными целевыми функциями,
* находить компромисс между метриками TTT, CO и LCS.

---

## 2. Реализованные методы и эксперименты

### 2.1. KMeans как приближение p-median (базовое решение)

В качестве стартового решения использовалась кластеризация клиентов с помощью KMeans:

* число кластеров = 250;
* расстояние — евклидово;
* центры кластеров интерпретируются как координаты клиник.

**Почему это работает:**

* KMeans минимизирует сумму квадратов расстояний;
* даёт хорошее приближение при большом числе клиентов;
* работает быстро и масштабируется.

**Недостатки:**

* не учитывает вместимость клиник;
* не учитывает дополнительные признаки напрямую;
* может приводить к сильной перегрузке отдельных клиник.

---

### 2.2. Жадное назначение клиентов с учётом вместимости

Для оценки качества решений и учёта ограничений был реализован жадный алгоритм назначения:

* каждый клиент назначается ближайшей клинике;
* если клиника переполнена, клиент переназначается следующей по расстоянию.

Это приближает задачу к **Capacitated Facility Location Problem** и позволяет:

* явно считать перегрузку,
* исключать штраф CO при корректном распределении.

---

### 2.3. Штрафная функция за перегрузку

Была реализована штрафная часть целевой функции:

[
y \cdot \sum_{j=1}^{N} \max(0, K_j - L)
]

Она используется:

* как метрика качества,
* как компонент будущей оптимизации,
* как инструмент сравнения решений.

---

### 2.4. Feature Scaling и трансформации

Для корректной работы с признаками были протестированы различные методы масштабирования:

| Метод          | Назначение                         |
| -------------- | ---------------------------------- |
| MinMaxScaler   | унификация диапазонов              |
| StandardScaler | центрирование и нормализация       |
| log1p          | устранение heavy-tail              |
| Shift + log    | работа с отрицательными значениями |

Это позволило стабилизировать модели и подготовить данные для обучения.

---

### 2.5. Автоматический подбор весов LCS — ключевой прорыв

Для определения важности признаков были протестированы:

1. **Корреляционный анализ**
   → столкнулись с проблемой нулевой дисперсии и NaN.

2. **Линейная регрессия**
   → коэффициенты оказались нестабильными.

3. **Ridge Regression**
   → регуляризация (α ≈ 1e-6) стабилизировала веса.

Дополнительно:

* отрицательные веса отсекались;
* веса нормализовывались до суммы = 1.

📌 Этот шаг стал **ключевым прорывом**, устранив NaN и позволив автоматически определять вклад признаков.

---

## 3. Фактически применённые концепции

В процессе работы были реализованы (пусть и частично):

* многокритериальная оптимизация;
* feature engineering;
* регуляризация;
* оценка плотности;
* эвристическая оптимизация;
* мягкие ограничения (через штрафы и веса);
* взвешенная кластеризация.

---

## 4. Идеи и методы для дальнейшего развития

### 4.1. Учет вместимости на уровне оптимизации

* capacitated k-means;
* iterative reassignment клиентов;
* soft assignment с балансировкой;
* min-cost flow для распределения клиентов.

---

### 4.2. Альтернативные методы оптимизации

* **K-Medoids** — устойчивость к выбросам, реальные центры;
* **Simulated Annealing** — выход из локальных минимумов;
* **Genetic Algorithms** — работа со сложным ландшафтом;
* **Particle Swarm Optimization** — непрерывные координаты;
* **Tabu Search / Basin Hopping** — гибридные подходы.

---

### 4.3. Feature-based placement

Использование признаков для:

* построения тепловых карт,
* генерации кандидатов в «хороших» зонах,
* комбинации с кластеризацией.

---

### 4.4. Взвешивание клиентов

Идея:

* пожилые и уязвимые клиенты получают больший вес;
* расстояние для них становится более значимым.

---

### 4.5. Многокритериальная оптимизация по Pareto

Вместо одной функции:

* минимизация TTT,
* минимизация CO,
* максимизация LCS.

Это позволяет выбирать компромиссные решения.

---

### Итоги по метрикам:
<img width="555" height="88" alt="image" src="https://github.com/user-attachments/assets/9ad65e74-cc01-4238-a9ca-3baeb4788d45" />

<img width="431" height="114" alt="image" src="https://github.com/user-attachments/assets/8154d404-cacd-4876-9887-753d149b2e85" />

## 5. План эволюции решения

1. **KMeans → перераспределение клиентов**
2. **Учёт вместимости + штрафы**
3. **Добавление социальных и экологических признаков**
4. **Метаэвристики (SA / GA / PSO)**
5. **Ансамблирование решений**
6. **Финальный гибридный алгоритм**

---

## 6. Долгосрочные перспективы

* ансамблирование планировок;
* stacking и meta-learning;
* SHAP-анализ важности признаков;
* пространственная валидация;
* использование дорожной сети вместо евклидового расстояния;
* transport-weighted и time-based distance.

---

### Итог

На текущем этапе:

* построен корректный baseline;
* реализована формальная оценка качества;
* устранены численные проблемы;
* заложена архитектура для масштабируемого улучшения решения.

Дальнейшая работа — это **переход от эвристики к системной оптимизации**, с использованием гибридных и обучаемых методов.

