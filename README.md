# 🏥 Оптимизация расположения клиник с ограничением нагрузки

## 📌 Обзор проекта

Проект решает задачу **оптимизации расположения объектов (клиник) с ограничением на максимальную нагрузку**.
Даны координаты клиентов, необходимо разместить фиксированное число клиник на плоскости так, чтобы **минимизировать нелинейную функцию расстояний** и штраф за перегрузку клиник.

Решение разрабатывалось на хакатоне по машинному обучению с фокусом на **очень точную оптимизацию**, где улучшения считаются в миллионных долях метрики.

---

## 🎯 Цель

Разместить **250 клиник** так, чтобы минимизировать метрику:

[
T = \mathbb{E}[\alpha d + \beta d^2] + \gamma \sum \max(0, \text{load}_k - L)
]

где:

* (d) — расстояние от клиента до ближайшей клиники
* (\alpha, \beta) — коэффициенты стоимости расстояния
* (\gamma) — коэффициент штрафа за перегрузку
* (L) — максимальная нагрузка на клинику

---

## 🧠 Использованные методы

Решение строится через **многоступенчатый пайплайн**, объединяющий:

1. **Stage 0 — Инициализация**

   * Случайное размещение клиник по координатам клиентов.

2. **Stage 1.5 — Мягкое распределение (Soft Assignment, Annealed)**

   * Мягкое присвоение клиентов клиникам с температурным спуском.
   * Оптимизация сглаженной версии целевой метрики.
   * GPU-ускорение с PyTorch.

3. **Stage 2 — Оптимизация с учётом нагрузки (Softplus Capacity Refinement)**

   * Введение мягкого штрафа за перегрузку через softplus.
   * Балансировка нагрузки между клиниками.

4. **Stage 6++ — Дискретная оптимизация на границах**

   * Работа с **жёстким присвоением (HARD assignment)**.
   * Выделение клиентов на границах Вороного.
   * Микросдвиги центров и принятие только улучшений.

Этот этап критичен для достижения **лучшего возможного значения метрики**.

---

## ⚙️ Техническая реализация

* **Фреймворки**: PyTorch, NumPy, SciPy, Pandas
* **Ускорение**: CUDA (если доступна)
* **Точность**: Float32 с стабилизацией
* **Метрика**: Жёсткое присвоение (HARD)

---

## 📈 Результаты

| Показатель            | Значение         |
| --------------------- | ---------------- |
| **Лучший HARD T**     | **≈ 0.05557947** |
| Клиентов              | ~42,000          |
| Клиник                | 250              |
| Максимальная нагрузка | 1000             |

Решение достигает **локального оптимума**, где дальнейшие методы улучшения практически не дают эффекта.

---

## 🚧 Ограничения и наблюдения

* Ландшафт оптимизации содержит много плоских областей и резких локальных минимумов.
* Когда границы Вороного стабилизируются, улучшения становятся крайне трудными.
* Дискретная оптимизация по границам превосходит чисто градиентные методы на поздних этапах.

---

## ▶️ Как запускать

1. Установить зависимости:

```bash
pip install -r requirements.txt
```

2. Поместить исходные данные:

```text
clinic-data.csv
```

3. Запустить ноутбук:

```bash
jupyter notebook
```

4. Выполнить все ячейки сверху вниз.

Финальный результат будет сохранён в:

```text
final_solution.csv
```

---

## 📁 Структура репозитория

```text
.
├── clinic-data.csv          # координаты клиентов
├── notebook.ipynb           # полный пайплайн оптимизации
├── final_solution.csv       # финальные координаты клиник
└── README.md
```

---

## 🔬 Основные выводы

* Гибридный подход (soft + hard + дискретные границы) эффективнее каждого метода отдельно.
* GPU-ускорение критично при большом числе клиентов.
* Этап Stage 6++ позволяет «выжать» последние доли улучшения.
